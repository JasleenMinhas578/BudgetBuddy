# BudgetBuddy – Claude Instructions

## AI / Gemini Model

- Always use `gemini-3.6-flash` as the Gemini model — never `gemini-2.0-flash` or any older version.
- The model name is defined in `src/services/aiService.js` as `GEMINI_MODEL`.
